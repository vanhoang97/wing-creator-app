import React from "react";
import ReactDOM from "react-dom/client";
import WingChatIntroApp from "./WingChatIntroApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WingChatIntroApp /> {/* ✅ Sửa đúng tên component */}
  </React.StrictMode>
);
